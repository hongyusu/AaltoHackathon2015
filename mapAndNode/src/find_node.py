#!/usr/bin/python

from data_preprocess import nodeInfoPath, mapInfoPath, center

NodeList = {}

def find_node(lng, lat):
        searchKey = "%s,%s"%(center(lng), center(lat))

        #read the existing cener points
        with open

print nodeInfoPath
print mapInfoPath
